import { FilterItemByNamePipe } from './filter-item-by-name.pipe';

describe('FilterItemByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
